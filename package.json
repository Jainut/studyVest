{
  "name": "studyvest",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "demo": "concurrently -n DEMO,WEB -c yellow,magenta \"node tools/demo-api.mjs\" \"npm run dev -w frontend\"",
    "build": "npm run prisma:generate -w backend && npm run build -w backend && npm run build -w frontend",
    "check": "npm run typecheck -w backend && npm run typecheck -w frontend",
    "db:generate": "npm run prisma:generate -w backend",
    "db:push": "npm run prisma:push -w backend",
    "db:migrate": "npm run prisma:migrate -w backend",
    "db:seed": "npm run seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
